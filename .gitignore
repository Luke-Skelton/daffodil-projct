# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, for example:
*.out

# Dependency directories (remove these if you are using Go modules)
vendor/

# Example of an env file.
.env